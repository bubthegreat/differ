apiVersion: v1
kind: ConfigMap
metadata:
  name: differ-api-config
data:
  DB_HOST: "mysql"
  DB_PORT: "3306"
  DB_USER: "root"
  DB_NAME: "skillmatrix"
  BYPASS_AUTH: "true"
  JWT_SECRET_KEY: "local-dev-secret-key"
  CORS_ORIGINS: "http://localhost:3000,http://localhost:4200,http://app.localhost"
